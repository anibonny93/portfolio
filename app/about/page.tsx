import Image from 'next/image';
import { profile, education } from '@/data/portfolio';
import { asset, metadata } from '@/lib/site';
import { ContactBand, PageIntro } from '@/components/shared';
export const generateMetadata=()=>metadata('About','Mechanical engineering, industrial operations and sustainable materials research.', '/about');
export default function About(){return <div className="shell"><PageIntro eyebrow="About Aniruddha" title="Engineering across scales." description="From plant operations and manufacturing quality to molecular systems and public infrastructure."/><div className="about-layout"><div className="prose">{profile.bio.map(p=><p key={p}>{p}</p>)}<section><h2>Education</h2>{education.filter(e=>e.id!=='pe').map(e=><div className="education" key={e.id}><h3>{e.title}</h3><p>{e.institution}</p><p>{e.detail}</p></div>)}</section></div><div className="about-photo"><Image src={asset(profile.headshot)} alt={profile.headshotAlt} width={600} height={720} sizes="(max-width:760px) 85vw, 320px"/><p>{profile.location} · {profile.relocation}</p></div></div><section className="timeline-section"><p className="eyebrow">Career & education</p><h2>A connected path.</h2><ol className="timeline">{profile.timeline.map(item=><li key={item.title}><span>{item.when}</span><div><h3>{item.title}</h3><p>{item.detail}</p></div></li>)}</ol></section><ContactBand/></div>}
